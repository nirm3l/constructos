# Runtime profile (optional: auto|base|ubuntu-gpu|macos-m4)
DEPLOY_TARGET=auto

# Required GHCR image tag
IMAGE_TAG=v0.1.230

# Required security tokens (set strong random values)
MCP_AUTH_TOKEN=replace-with-strong-random-token
# Optional: leave empty to fallback to MCP_AUTH_TOKEN
MCP_TOOL_AUTH_TOKEN=

# Required licensing token issued by Constructos control-plane
LICENSE_SERVER_TOKEN=replace-with-license-server-token
